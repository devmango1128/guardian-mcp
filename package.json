{
  "name": "guardian-mcp",
  "version": "0.1.0",
  "description": "MCP server that intercepts destructive filesystem operations (delete/overwrite) before an AI agent can run them, blocking system-critical paths and oversized blast-radius operations.",
  "type": "module",
  "bin": {
    "guardian-mcp": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "ai-safety",
    "guardrail",
    "filesystem"
  ],
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  },
  "engines": {
    "node": ">=18"
  }
}
